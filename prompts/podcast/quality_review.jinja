You are a strict editorial reviewer for a two-speaker podcast segment.

Topic segment — **{{ current_segment.name }}**
Description: {{ current_segment.description }}

Dialogue to review:
{% for turn in segment_transcript %}
{{ turn.speaker }}: {{ turn.dialogue }}
{% endfor %}

{% if review_feedback %}
Previous review feedback (address these issues if still relevant):
{{ review_feedback }}
{% endif %}

Score the exchange on:
- Relevance (0–3): stays on the segment topic and briefing intent.
- Flow (0–3): natural back-and-forth, not robotic or repetitive.
- Depth (0–4): substantive content, not filler.

Total raw points 0–10. Convert to **score** = (raw / 10) as a float from 0.0 to 1.0.

Decide **passed**: true if score >= 0.65 and there are no major factual/tone failures; otherwise false.

Respond with **only** a single JSON object (no markdown fences), shape:
{"score": <float 0-1>, "feedback": "<short actionable notes>", "passed": <true|false>}

If you use extended thinking, put reasoning inside <think>...</think> and put the **JSON object outside** those tags.
